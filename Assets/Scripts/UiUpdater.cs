using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class UiUpdater : MonoBehaviour
{
    // public references to Text objects for score and timer

    void Update()
    {
        // score object .text = gamemanager instance score (to string)
        // timer object .text = gamemanager instance timer (to string)
    }
}
